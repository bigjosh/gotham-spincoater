# Bench UI validation — 2026-09-08

Board: Raspberry Pi Pico 2 W, MicroPython v1.29.0, USB ID 8792b44d9c11021d, Windows COM7. COM4/COM5 were excluded and never opened.

- All six application Python files uploaded, with SHA-256 readback matching the local files.
- Application initialized ST7796S SPI display, buttons, joystick, GP18 stopped output, GP19 pull-up and tach IRQ without exceptions. A timed three-second run exited cleanly and commanded stopped output.
- Eleven host tach/control tests passed, including exact 3,000 RPM, timing/counter wraps, bounce rejection, startup and timeout.
- PIO backend was assembled with official MicroPython v1.29.0 assembler by the implementation agent and its six instruction words emulated across all 998 intermediate duty settings. Every period was 2,000 clocks; no output-high instructions were used.
- Board synthetic waveform test on GP0 at 50%: median high 19 μs, low 21 μs, total 40 μs; configured/calculated frequency 25,000 Hz. The pulse measurements use the Pico's own clock, not an external calibrated oscilloscope.
- Synthetic 100 Hz PIO signal on GP0 was captured by the real GPIO tach interrupt: 101 accepted pulses, 99.9996 Hz, 2,999.988 RPM. Constant-low and released endpoints, followed by no-pulse timeout, passed.
- Test resources on GP0/GP1 were released, then the normal application was relaunched at 0% output and left running. Startup telemetry reported no tach pulses.

These synthetic tests validate software and the board's internal signal path. Physical TFT readability/orientation and actual fan wiring/RPM require user observation. No actual fan maximum speed has been measured in this session.

## User-requested active-drive / 10 kHz experiment — 2026-09-08

- Configured GP18 for noninverted hardware PWM (`PWM_PUSH_PULL=True`, `PWM_HZ=10000`); GP19 remains tach with `Pin.PULL_UP`. Tach processing is unchanged for comparison.
- All 15 host tests passed, including four new regressions covering push-pull startup/frequency, 0/25/50/100% positive duty and clamps, LOW shutdown/IRQ cleanup, and rejection of nonpositive frequencies. This covers software behavior, not external signal integrity.
- Deployed all six Python files to the identified board on COM7 with SHA-256 readback verification and backups of previous files. COM4/COM5 were excluded.
- `artifacts/verify-push-pull-10khz.py` verified a real `machine.PWM` instance with frequency readback 10000 Hz, duty readback 0, GP18 LOW, GP19 input/pull-up, and GP18 remaining LOW after close. It never requested nonzero duty. No oscilloscope measurement of waveform voltage/timing was made.
- Relaunched the UI with `FAN_UI_READY PWM=GP18 TACH=GP19 MODE=PUSH_PULL HZ=10000 OUTPUT=0%`. Screen labels now identify `PUSH-PULL`, `10kHz`, and fan `00`.
- At startup, telemetry reported approximately 2,745-2,749 RPM despite the 0% command. The current physical PWM-wire connection was not confirmed, so this does not establish that the fan obeys the new drive or stops at 0%. The application was left running for user-controlled testing.
- 10 kHz is below ARCTIC's documented 21-28 kHz range. The false tach problem is not claimed fixed by this deployment.

## PWM-synchronized tach sampling — 2026-09-08

- Installed all ten application Python files with SHA-256 readback verification and backups. Target remained USB serial 8792b44d9c11021d on COM7. COM4/COM5 were excluded throughout.
- 57 host tests passed: 22 fan control/ownership, 10 rolling period measurements, 12 rig configuration/lifecycle, six PIO instruction timing/counting, seven DMA pacing/lifecycle. Host PIO tests interpret the decorated source; real MicroPython assembled and executed it on the board.
- The 17-instruction PIO program samples once per hardware PWM wrap, delayed to the midpoint of the longer phase, then reports exact full falling-to-falling periods in 100-microsecond units. Six readers use SM0/1/2/3/8/9 and six DMA channels. Shared-wrap readers use one PWM DREQ consumer followed by unpaced chained transfers.
- On-board tests caught and fixed MicroPython's lack of array multiplication, and a synthetic generator's out-of-range SET immediate. Confirmed PIO0/2 state machines were all inactive before reclaiming instruction memory left allocated by earlier experiments. The new sampler removes its own program after its last user closes; Launch also releases exact known program objects before unloading modules.
- `tools/test_sync_board.py` drove only spare GP0/GP1. At 25%, 50%, and 75% PWM, six simultaneous samplers each reported exactly 10000 microseconds / 100 Hz / 3000 RPM from a synthetic tach waveform with approximately 3-microsecond glitches at both PWM edges. Each eight-period window filled. Selected ideal sample offsets were 62.5, 25.0, and 37.5 microseconds after wrap. No external oscilloscope timing calibration was performed.
- No-pulse timeout passed after 1.6 seconds. An independent clean 100 Hz generator then verified actual period capture at constant 0% and 100% PWM on all six samplers. Closing the first shared-wrap reader preserved readings on the other five. All test DMA, PIO programs, and GP0/GP1 outputs were released. Full transcript: `sync-board-test.txt`.
- UI launched with `TACH_SYNC=True CHANNELS=(0,) AVERAGE_PERIODS=8`, hardware push-pull 10000 Hz, fan #0 on GP18/GP19, 0% output. Startup reported no tach periods / invalid measurement, not a measured zero shaft speed. Transcript: `sync-ui-launch.txt`.
- Synthetic tests demonstrate capacity and rejection of the injected edge-local disturbances. Actual fan waveform integrity and maximum speed are not established by this test. The manual UI currently resets the affected slice's averaging window when duty changes; frequent closed-loop ramp updates need further work. 10 kHz remains below ARCTIC's specified range.

## Real-fan confirmation — 2026-09-08

After trying the deployed synchronized sampler, the builder reported: **"dude, problem 100% solved!"** This confirms resolution of the reported false-RPM problem on the actual bench fan. No new speed calibration, maximum-RPM value, or six-physical-fan result was supplied. The problem, the builder's timing insight, and the implementation are documented in [The Genius Move](../THE_GENIUS_MOVE.md).

## Combined PIO, RPM seeking, recipes and Wi-Fi — 2026-09-08

The application now uses one combined PIO machine and one DMA channel per enabled fan. Each machine generates 10 kHz PWM, samples tach at the midpoint of its longer phase, counts full tach periods, and latches LOW on STOP or command starvation. Core 1 owns the drivers and runs independent RPM seekers at a nominal 50 Hz; core 0 handles the six-card TFT, captive HTTP/DNS portal, and idle recipe storage. Only fan #0 is enabled on GP18/GP19.

- **147 host tests pass.** These cover encoded PIO timing, hardware-register/lifecycle behavior, recipes and bounded correction, startup/coasting/dropout cases, FIFO overflow versus quiet detection, core shutdown and START/edit races, HTTP/DNS, and browser-script interactions, alongside the legacy tests.
- **Current driver on the board:** synthetic 100 Hz tach produced exactly 3,000 RPM at 0%, 25%, 50%, 75%, and 100% PWM. Repeated internal high/low pulse measurements were 26/75 µs, 50/50 µs, and 76/26 µs at 25%, 50%, and 75%; these are self-clocked measurements, not external oscilloscope calibration. Sixty changing duty commands preserved valid tach data. STOP stayed LOW after release until explicit rearm. Disabling command DMA parked LOW and reported `command_underrun`. Intentional RX FIFO overflow invalidated old measurements and recovered with new periods.
- **Six-engine resource test with Wi-Fi:** six simultaneous state machines/DMA channels in PIO0/PIO2 each read 3,000 RPM with full eight-period windows and no new overflow, while an open AP remained active. All six hardware stop latches parked before Python serviced them. This used shared dummy GP0 PWM and GP1 tach, plus GP20 as a simulated STOP; it proves concurrent resource/capture operation, not six independent physical waveforms or six motors. [Transcript](capacity-board-test.txt).
- **Real fan, speed changes:** an initial 0.02 correction gain caused substantial low-speed startup overshoot. The final 0.01 gain retained the ceiling of 10 PWM percentage points/s and completed 0 → 1,200 → 2,400 → 1,600 → 3,000 → 0 RPM, including a zero-slew downward target jump. Startup still has a transient at 1,200 RPM; the measured hold eventually settles in the agreed ±50 RPM band. There were no tach FIFO overflows. [Final-gain transcript](seeker-board-test.txt).
- **Complete application with real wireless traffic:** the default 15 s up / 30 s dwell / 15 s down recipe completed. In the final run, the 3,000 RPM dwell samples were about 2,952–2,963 RPM at 97.6–97.7% requested power, inside ±50 RPM. Recipe elapsed time was 73.4 s including settling; a 20 s test setup window preceded START. Maximum observed control-loop lateness was 6 ms, with no tach FIFO overflows. The TFT updated throughout, core 0 performed explicit garbage collection, and an external Wi-Fi client repeatedly requested status and the full 15.8 KB editor through the ramp and over 20 seconds of the hold. [Transcript](app-board-test.txt).
- **Portal:** Windows joined the password-free Gotham Spinner AP using an otherwise idle Wi-Fi adapter while Ethernet retained its primary route. Actual HTTP config save/readback, full page transfer, wildcard DNS, captive-probe redirect, and HTTP 409 edit rejection during a recipe passed. The final stress client completed 54 repeated status/page requests, with observed hold readings of 2,952–2,963 RPM and no tach overflow. [Transcript](wifi-portal-test.txt). The first harness outlived the test's deliberate AP shutdown; its termination condition now finishes during the hold. A separate truncated-response failure exposed the 2-second idle timeout expiring during long display redraws. The dashboard now services the portal between drawing batches, and the idle timeout is 5 seconds; the final over-the-air retest passed.
- **Editor:** actual Chrome localhost checks covered creation, copying, deletion, step editing, rename/save, selected recipe saving, JSON export, and busy-state lockout. Export produced valid JSON. Automated native file selection for import was unavailable; the actual import JavaScript passed its host test. The Pico has also served the same self-contained editor over its real AP.

Implementation checks on hardware caught unsupported string-form injected JMP instructions and the absence of `socket.getsockname` in this MicroPython build; both are corrected and covered by regressions. Worker heartbeat grace now starts at thread launch, after potentially slow display initialization. Fatal shutdown asserts the shared active-low STOP GPIO without touching DMA from the wrong core, then waits for worker acknowledgement. Persistent FIFO overflow counts as tach activity and cannot falsely establish a quiet zero-speed step.

At zero power, absent tach is reported as invalid and quiet is explicitly inferred, not measured shaft standstill. The 3,000 RPM result depends on the existing supply/fan and the configured two tach pulses per revolution; it is not an independent tach calibration or a measured absolute maximum. Six physically wired motors remain untested. The user's successful 10 kHz bench setting remains below ARCTIC's stated 21–28 kHz range.

After testing, the temporary PC Wi-Fi profile was removed and the adapter restored to disconnected; Ethernet stayed up. The Pico was rebooted into the normal application at zero power with only fan #0 enabled. The helper continued to identify only USB serial 8792b44d9c11021d on COM7; COM4/COM5 were never opened. Saved user recipes are separate from the disposable test recipe file.

## Four channels on the unmodified kit — 2026-09-08

Enabled fans **#0–#3** using PWM/tach pairs **GP18/19, GP20/21, GP22/28, and GP0/1**. Both remaining pairs use occupied kit GPIOs, so #4/#5 remain disabled and `AUX_LINKS_DISCONNECTED` remains `False`. No control algorithm, pin allocation, PIO code, or recipe data changed.

- **147 host tests passed**, including the updated shipped-configuration check for four distinct fan pairs and existing allocation, auxiliary-link guard, control, STOP, and dashboard tests.
- Deployed only `config.py`, with SHA-256 readback verification, to project Pico USB serial **8792b44d9c11021d** on COM7. [Upload transcript](four-fan-deploy.txt), [reboot transcript](four-fan-reboot.txt).
- Normal startup reported `CHANNELS=(0, 1, 2, 3)` with PIO 10 kHz and core-1 50 Hz control. The eight-second monitor showed all four channels idle at **0% requested power**, no valid tach, and a reported maximum loop lag of 0 ms during that observation. [Startup transcript](four-fan-startup.txt).
- No recipe or motor-spin test was initiated for this change; physical wiring and spin behavior of fans #1–#3 remain unverified. All enabled channels must be connected before START, or the missing-tach protection will fault the run. COM4 and COM5 were not opened.

## Persistent per-fan touch selection — 2026-09-08

Added ENABLE/DISABLE buttons to the six-card TFT. Disabled cards have a red background and their channels are excluded from regulation, settling, dwell, and tach-loss checks. The browser mirrors the red disabled state. Selections persist separately in `fans.json`; channels #4/#5 remain locked while the kit's auxiliary GPIO connections are fitted. Selection changes are idle-only, all-off is permitted, and START refuses an empty selection.

- **219 host tests passed.** New coverage includes GT911 packet decoding and coordinate rotation, one event per press, held/multitouch/error suppression, button hit regions and red cards, touch-to-controller routing, settings persistence/recovery, all-off behavior, disabled-channel exclusion, GPIO availability, and START/STOP/edit races. A press that begins during a settings save must be released before a fresh START can be accepted.
- The real dashboard draw calls were rendered with an approximate host font and visually inspected in [the updated preview](dashboard-preview.png). This validates layout, not physical touch alignment or panel colors.
- Touch protocol, address, and reset sequencing follow the [manufacturer's Pico 2 example](https://github.com/geeekpi/pico_breadboard_kit/tree/pico2) and [Goodix driver](https://github.com/goodix/gt9xx_driver_android/blob/master/gt9xx.c). The driver expects GT911 at address `0x5D`, native 320×480 coordinates, and rotates them to the current landscape TFT. It does not rewrite the controller configuration.
- **Deployment and physical verification are pending:** the project USB device was disconnected during this change. The identity guard stopped before opening a serial port; COM4 and COM5 were not accessed. The last deployed firmware remains the preceding four-channel version. No new motor run was initiated.
- Prepared [the zero-power board integration check](../tools/test_fan_settings_board.py) for saved selections, worker acknowledgements, GPIO locks, and LOW output levels. Run it after reconnection/deployment, then confirm that a tap toggles the intended tile once and that the selection survives reboot.

## Auxiliary GPIO modifications confirmed — 2026-09-08

The builder reported completing the GPIO modifications and requested unlocking fans #4/#5. Set `AUX_LINKS_DISCONNECTED = True`; all six channels are now hardware-available to the runtime and touchscreen. The initial selection stays `(0, 1, 2, 3)`, and saved `fans.json` selections are preserved. Fans #4/#5 offer ENABLE rather than LOCKED until selected.

All **219 host tests passed**, including the updated shipped-configuration expectation and explicit unmodified-kit guard tests. Updated the header map, modification status, and rendered dashboard preview. No new electrical or motor test was performed; the project Pico remains disconnected, so deployment and physical touch verification are still pending.

## Adjacent signal pairs with PWM below tach — 2026-09-08

Reassigned fans #3–#5 so their PWM pin sits immediately below their tach pin on the same header, viewed with the Pico USB connector at the top. PWM/tach pairs are now **#0 GP18/19, #1 GP20/21, #2 GP22/28, #3 GP1/0, #4 GP13/12, #5 GP16/17**. Fan #2 retains its nonadjacent pair as requested; fans #0/#1 already met the placement rule. Fan numbers, saved selections, recipes, and PIO state-machine allocation are unchanged.

All **220 host tests passed**, including a new check against physical header positions and updated six-channel driver/runtime fixtures. Documentation now groups RGB/buzzer isolation under fan #4 and D1/D2 isolation under fan #5. This pin map has not been deployed or tested electrically; fans #3–#5 require rewiring with power disconnected before deployment.

## Latest firmware deployed and selection checked on Pico — 2026-09-08

Deployed all device Python modules from **`b77ae46`** to project Pico USB serial **8792b44d9c11021d**, identified on COM7. Each uploaded file passed SHA-256 readback verification before activation; `main.py` was activated last. Existing `fans.json` and `recipes.json` were preserved. COM4 and COM5 were not opened. [Upload transcript](touch-selection-deploy.txt).

- **Normal startup passed:** detected GT911 product `911`, firmware 4192, address `0x5D`, raw resolution 320×480, and display rotation 1. The TFT/controller/AP application reported ready with no startup exception. [Initial startup](touch-selection-first-startup.txt).
- **On-board fan-selection integration passed:** exercised empty, single, multiple, all-six, and #4/#5-only selections through the live core-1 acknowledgement path. Each selection reloaded correctly from its separate temporary settings file. All six PWM GPIOs read LOW, the worker heartbeat remained current, and no recipe was started. Temporary files were removed afterward. [Test transcript](fan-selection-board-test.txt).
- **Final reboot passed:** the normal application restored the saved selection **`(0, 3)`** and remained IDLE with both selected fans at 0% requested power. The observed loop-lag maximum was 0 ms in this short idle sample. [Final startup](touch-selection-startup.txt).

This completes deployment of the touch-selection and adjacent-pin changes. The checks verify controller detection, selection persistence, control-core coordination, and zero output levels; physical touch alignment and positive-power operation of the rewired fans were not measured in this deployment session.

## Individual fan fault isolation — 2026-09-08

Fan faults now park only the affected output and exclude that channel from the current recipe. Healthy channels retain their ramp, individual settling history, and accumulated dwell. Missing tach, reach/recovery timeout, and local driver errors are latched per fan; fresh START retries the selected channels. If every participant fails, the run ends FAULT. Physical STOP, controller-clock changes, and failure to safely park an output remain global stops. Saved fan selections and recipes are unchanged.

- **242 host tests passed.** New coverage includes six-channel ramp continuation, independent settling/recovery deadlines, persistent tach loss, completion with faults, all-failed termination, retry clearing, driver arm/sample/power-command errors, physical STOP including brief latches, and isolated fault display. The actual dashboard draw calls were visually checked in [the fault preview](fan-fault-dashboard-preview.png).
- **Deployment verified:** uploaded `control.py`, `runtime.py`, `dashboard.py`, `webpage.py`, and `main.py` to USB serial **8792b44d9c11021d** on COM7. All five passed SHA-256 readback before activation. COM4/COM5 were not opened. [Upload transcript](fan-fault-deploy.txt).
- **Control engine on MicroPython passed:** scripted readings caused one PIO fault followed by a second fan's tach loss. Remaining participants retained earned dwell and completed the recipe. Fresh START cleared faults; a fault-free stopped latch still stopped the entire run. This test accessed no GPIOs. [Transcript](fan-fault-engine-board-test.txt).
- **Live core-1/PIO integration passed at zero power:** selected all six in memory with an all-zero-RPM test recipe and injected a `command_underrun` fault on fan #0 inside its owning core. Fan #0 parked while the other five PIO drivers remained armed across ten status observations. All six PWM GPIOs read LOW and the heartbeat stayed current. The controller closed every output LOW afterward. No saved files were read or changed by this test, and no positive-power command was issued. [Transcript](fan-fault-runtime-board-test.txt).
- **Final normal application:** restored the user's current all-six selection and remained IDLE at zero requested power, with the TFT/controller/AP startup reaching `GOTHAM_READY`. [Startup transcript](fan-fault-startup.txt).

**Touch diagnostic remains unresolved:** normal startup reported `TOUCH_UNAVAILABLE [Errno 5] EIO`. The unchanged touch driver also failed a standalone reset/identity read with the fan worker stopped; a scan of I2C0 on GP8/GP9 found no responding devices. SDA/SCL/reset read HIGH; INT read LOW. The normal app was relaunched afterward. This establishes that the touch controller was unresponsive during verification, not its cause. [Diagnostic transcript](fan-fault-touch-diagnostic.txt). Physical START/STOP and fan control remain available, but touchscreen enable/disable cannot work until communication is restored. No touch firmware, pin assignment, or hardware wiring was changed in this update.

These checks establish fault isolation in the engine and live zero-power runtime. They do not constitute a positive-power six-motor fault test.

## Recoverable RPM warnings and scheduled recipes — 2026-09-09

Replaced the application-level fan FAULT state with a timed RPM warning. A selected fan stays driven when off target. Default warning settings are **5% deviation continuously for 2 seconds**; the first in-bounds reading clears the warning and its timer without a new START. Each of six fans has an independent timer. The TFT and browser retain actual requested power and its bar while red. Disabled channels retain their separate red OFF presentation.

Slew and dwell now follow their scheduled durations even when RPM is outside bounds. The configured RPM deadband remains independent of the percentage warning and defines the zero-target band. Missing tach does not shut down a fan: unmeasured startup remains capped at 30%, established drive is held until readings return, and the existing low-power coast-down behavior remains. Physical STOP still actively drives every PWM LOW. Actual PIO resource errors remain a separate driver condition; fatal controller errors still stop all outputs.

The editor exposes the percentage threshold and delay, including a zero-second delay. Existing version-1 recipes migrate in memory from the exact old settings schema: custom profiles, power limit, and RPM deadband are preserved, new warning defaults are added, and obsolete settling/timeouts are removed. Files are not rewritten until an explicit save. Host persistence tests verify the original legacy file is retained as the backup after saving.

- **244 host tests passed**, covering warning boundaries, continuous timing, immediate recovery, continued drive, scheduled transitions, missing/invalid tach, STOP, driver-error isolation, settings migration, and browser editing/import/export. Recipe elapsed time accumulates in integer milliseconds; a 30-minute schedule tested with 17 ms updates still ends at the exact deadline.
- The actual dashboard draw calls were rendered and visually checked in [the RPM-warning preview](rpm-warning-dashboard-preview.png). The pictured RPM values are illustrative.
- [The scripted engine check](../tools/test_rpm_warning_board.py) passes under CPython: fan #0 warns while driven, recovers without START, then keeps its drive through nine seconds of missing tach. The recipe completes on its 20-second schedule. This check uses no GPIOs.
- [The live runtime warning check](../tools/test_rpm_warning_runtime_board.py) and [driver-error check](../tools/test_driver_error_board.py) are prepared for MicroPython. Both contain only zero-RPM recipe steps, preserve saved files, and inspect all PWM pads LOW. They have been reviewed but have not run on the board for this change.

**Deployment is pending:** the identity-guarded helper could not find project USB serial **8792b44d9c11021d**, and a read-only Windows device check also found no matching present device. No serial port was opened; COM4/COM5 were not accessed. The last deployed version remains **`acc5086`** until the project Pico reconnects and these modules are uploaded. No new physical motor test or touch test was performed in this change.

## Continuous RPM display and passive monitoring — 2026-09-09

Every fan tile now shows numeric RPM in IDLE, disabled, running, and STOP/coast-down states. The web editor's **Show zero below (RPM)** setting defaults to **60** and accepts 0–1,000. Suppression is strictly below the threshold: 59 displays 0 and 60 displays 60. Zero disables suppression of valid positive readings. Raw RPM remains available to the seeker; the same setting defines near-zero for zero-target warning checks. Missing or invalid tach also displays 0, with a browser **No recent tach** label; this is not proof of physical standstill. All three complete settings schemas import correctly, and an idle settings save updates the display without START.

Previously, stopping the driver also stopped tach acquisition. Passive mode now reuses each fan's combined PIO/DMA sampler while an RP2350 GPIO override actively holds its PWM pad LOW. The sampler's internal STOP input is overridden HIGH independently of the physical pad; only explicit START restores PWM operation with the real STOP input. No additional state machines, DMA channels, or PIO instructions are needed. STOP, edits, shutdown, and driver failures retain their output-parking behavior.

The register positions were checked against the [RP2350 GPIO definitions](https://raw.githubusercontent.com/raspberrypi/pico-sdk/master/src/rp2350/hardware_regs/include/hardware/regs/io_bank0.h). GPIO initialization order was checked against [MicroPython v1.29 PIO initialization](https://raw.githubusercontent.com/micropython/micropython/v1.29.0/ports/rp2/rp2_pio.c) and its [GPIO direction/function sequence](https://raw.githubusercontent.com/micropython/micropython/v1.29.0/ports/rp2/mphalport.h). Overrides are applied after initialization and before starting the state machine. Tests inspect raw pad status through `pwm_is_low()` because normal GPIO reads see the overridden internal HIGH input in passive mode.

- **271 host tests passed.** Coverage includes exact threshold boundaries, disabled/STOP readings, stale data, settings migration/import/export, idle settings acknowledgement, six passive channels, active-LOW transitions, held STOP at boot, explicit rearming, preserved coast-down measurements, and driver-error isolation. The hardware-free RPM-warning engine check also passed under CPython.
- Actual dashboard drawing calls were rendered and visually checked in [the continuous-RPM preview](continuous-rpm-dashboard-preview.png). The shown coast-down readings are illustrative.
- **Deployment completed after the board reappeared:** the helper identified USB serial **8792b44d9c11021d** on COM7 and observed the old application IDLE before upload. Seven changed Python modules passed SHA-256 readback and were activated together. This also deploys the previously pending recoverable-warning changes. Saved `fans.json` and `recipes.json` were untouched; COM4/COM5 were not accessed. [Upload transcript](continuous-rpm-deploy.txt).
- **Live passive monitoring check passed at zero power:** six real PIO/DMA drivers stayed in passive mode, raw PWM pads were actively LOW, disabled fans' scripted RPM remained visible, the 59/60 boundary passed, and two successive sets of scripted RPM reached the display pipeline while shared STOP was held LOW. No recipe started. [Transcript](continuous-rpm-board-test.txt), [test source](../tools/test_continuous_rpm_board.py).
- **Recoverable-warning checks passed on MicroPython:** the hardware-free engine test confirmed timed warning, immediate recovery, continued drive requests through missing tach, scheduled completion, and global STOP. The live zero-power core-1 test confirmed warning/recovery with all six drivers remaining armed and all pads LOW. [Engine transcript](rpm-warning-engine-board-test.txt), [runtime transcript](rpm-warning-runtime-board-test.txt).
- **Live driver-error isolation passed at zero power:** an injected fan #0 driver error parked only its output; the five peers remained armed with a current worker heartbeat and all six pads LOW. [Transcript](continuous-rpm-driver-error-board-test.txt).
- **Normal application restored:** startup reached `TOUCH_READY` and `GOTHAM_READY` with the user's all-six selection, showing numeric zeros at zero power. The initial short startup sample reached a 1 ms maximum loop lag; the final idle sample recorded 15 ms. [Initial startup](continuous-rpm-initial-startup.txt), [final idle observation](continuous-rpm-startup.txt).

These checks verify real GPIO output overrides, live PIO/DMA resource operation, and the core-1 display/control pipeline. Scripted RPM does not verify physical tach accuracy or actual six-fan coast-down. No positive-power motor test or physical touch-alignment test was performed in this update.

## Physical button label alignment — 2026-09-09

Replaced the wide LEFT/RIGHT footer panels with compact START and STOP labels centered at display x=160 and x=248, with respective widths of 88 and 80 pixels. The [manufacturer's straight-on photograph](https://wiki.52pi.com/images/7/7e/EP-0127-03.jpg) shows the active display at approximately image x=423–836 and button centers at x=560/636. Mapping into the 480-pixel display gives about x=159/248. The builder's 26.6 mm from the left and 44.5 mm from the right fit the outer glass edges; unequal bezel margins explain why applying those offsets directly to the lit display was inconsistent. These remain photo-derived alignment estimates.

All **12 dashboard tests passed**, and the real drawing calls were rendered and visually checked in the updated [dashboard preview](dashboard-preview.png). Only `dashboard.py` was deployed to the identity-verified project Pico on COM7, with SHA-256 readback passing. [Deployment transcript](button-label-deploy.txt). After reboot, `TOUCH_READY` and `GOTHAM_READY` appeared, the current saved selection `(0, 1, 2, 4, 5)` was retained, and all outputs remained at zero power. [Startup transcript](button-label-startup.txt). Recipes and fan selections were not edited; COM4/COM5 were not accessed. Physical label-to-button alignment has not been measured on the assembled unit.
